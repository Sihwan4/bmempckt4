image: registry.gitlab.com/pages/quarto:latest 

pages:
  script:
    - quarto render 
    - mv docs public

  artifacts:
    paths:
      - public
      
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH